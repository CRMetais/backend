spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

broker:
  exchange:
    name: relatorio.exchange
  queue:
    name: relatorio.queue
  routingKey:
    name: relatorio.key

server:
  port: 8080